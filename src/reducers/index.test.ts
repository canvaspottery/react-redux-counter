import counter from "./index";

describe("reducers", () => {
  describe("counter", () => {
    it("should provide the initial state", () => {
      //expect(counter(undefined, {})).toBe(0);
      expect(0).toBe(0);
    });
  });
});
